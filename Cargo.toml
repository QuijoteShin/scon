# Cargo.toml — workspace root
[workspace]
members = ["rs", "ext"]
resolver = "2"

[profile.release]
opt-level = 3
# LTO fat — sin impacto significativo vs lto=true en este proyecto (single crate dominante)
lto = "fat"
codegen-units = 1
